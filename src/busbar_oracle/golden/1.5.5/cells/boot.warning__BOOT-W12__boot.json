{"applied":["boot.pool-order","text.port"],"body":{"text":"<TS>  WARN heterogeneous pool: cross-protocol failover translates via the IR and may not preserve all provider features pool=oracle-fo protocols=anthropic+openai\n<TS> ERROR auth is DISABLED: no `auth:` block in config \u2014 busbar is running as an OPEN RELAY (anyone can use it). Add `auth:` with `chain: [keys]` (and mint virtual keys via the admin API) before exposing it; do not run this in production\n<TS>  WARN auth.chain is empty (open relay) - only acceptable for dev; reject in production\n<TS>  WARN store: in-memory (ephemeral) - keys, groups' usage, and ledgers reset on restart; configure a durable store plugin for persistence\n"},"effects":{"exec_rules":["exec.ansi","ts.unix"],"stderr":"[warn] could not enable jemalloc background purge thread (`name` or `mib` specifies an unknown/invalid value.); enabling busbar's idle purge fallback so RSS still returns to idle after a load burst\n[warn] BUSBAR_PROVIDERS is DEPRECATED; set `providers_file:` in config.yaml instead (it is honored for now).\n  model m-anthropic via anthropic (http://127.0.0.1:<PORT>) max unbounded\n  model m-bedrock via bedrock (http://127.0.0.1:<PORT>) max unbounded\n  model m-cd-lane via openai-chat (http://127.0.0.1:<PORT>) max unbounded\n  model m-cohere via cohere (http://127.0.0.1:<PORT>) max unbounded\n  model m-gemini via gemini (http://127.0.0.1:<PORT>) max unbounded\n  model m-lane-c1 via openai-chat (http://127.0.0.1:<PORT>) max 1\n  model m-openai-chat via openai-chat (http://127.0.0.1:<PORT>) max unbounded\n  model m-openai-responses via openai-responses (http://127.0.0.1:<PORT>) max unbounded\n  model m-queue-lane via openai-chat (http://127.0.0.1:<PORT>) max 1\nbusbar: 9 models, 7 pools\n  pool /oracle-cd = [m-cd-lane] aggregate 2305843009213693951\n  pool /oracle-fb = [m-cohere] aggregate 2305843009213693951\n  pool /oracle-fo = [m-openai-chat, m-anthropic] aggregate 4611686018427387902\n  pool /oracle-lb = [m-gemini] aggregate 2305843009213693951\n  pool /oracle-lc1 = [m-lane-c1] aggregate 1\n  pool /oracle-q = [m-queue-lane] aggregate 1\n  pool /oracle-unused = [m-openai-chat] aggregate 2305843009213693951\n[error] auth is DISABLED: no `auth:` block in config \u2014 busbar is running as an OPEN RELAY (anyone can use it). Add `auth:` with `chain: [keys]` (and mint virtual keys via the admin API) before exposing it; do not run this in production\n"},"headers":{},"status":0}

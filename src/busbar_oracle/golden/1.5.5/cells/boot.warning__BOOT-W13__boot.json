{"applied":[],"body":{"text":""},"effects":{"exec_rules":["exec.ansi","ts.unix"],"stderr":"[warn] could not enable jemalloc background purge thread (`name` or `mib` specifies an unknown/invalid value.); enabling busbar's idle purge fallback so RSS still returns to idle after a load burst\n[warn] BUSBAR_PROVIDERS is DEPRECATED; set `providers_file:` in config.yaml instead (it is honored for now).\n[info] removed 1 orphaned plugin staging dir(s) left by a crashed prior run\n  model m-openai-chat via openai-chat (http://127.0.0.1:1) max unbounded\nbusbar: 1 models, 0 pools\n[error] auth is DISABLED (auth.chain is empty) \u2014 busbar is running as an OPEN RELAY; do not run this in production\n<TS> ERROR auth is DISABLED (auth.chain is empty) \u2014 busbar is running as an OPEN RELAY; do not run this in production\n<TS>  WARN auth.chain is empty (open relay) - only acceptable for dev; reject in production\n[error] durable governance store contains 1 key(s) but auth.chain does not name the `keys` verifier \u2014 those keys are INERT and NOT enforced (per-key budget / RPM / TPM / allowed_pools are bypassed; no data-plane request resolves them). Add `keys` to auth.chain to enforce them.\n<TS> ERROR durable governance store contains 1 key(s) but auth.chain does not name the `keys` verifier \u2014 those keys are INERT and NOT enforced (per-key budget / RPM / TPM / allowed_pools are bypassed; no data-plane request resolves them). Add `keys` to auth.chain to enforce them.\n"},"headers":{},"status":0}

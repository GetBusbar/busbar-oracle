{"applied":[],"body":{"json":{"booted":true,"mutation_body":{"error":{"code":"invalid_request","message":"config mutation refused: this busbar has no writable config overlay. This is expected when `config.locked: true` (an immutable/GitOps deployment) \u2014 edit config.yaml and POST /config/reload to change it. If the config is meant to be mutable, give it a writable `config.overlay` backend and reload."}},"mutation_status":"400","posture_line_present":false}},"effects":{"booted":true,"mutation_body":{"error":{"code":"invalid_request","message":"config mutation refused: this busbar has no writable config overlay. This is expected when `config.locked: true` (an immutable/GitOps deployment) \u2014 edit config.yaml and POST /config/reload to change it. If the config is meant to be mutable, give it a writable `config.overlay` backend and reload."}},"mutation_status":"400","posture_line_present":false,"stderr":"[warn] could not enable jemalloc background purge thread (`name` or `mib` specifies an unknown/invalid value.); enabling busbar's idle purge fallback so RSS still returns to idle after a load burst\n[warn] BUSBAR_PROVIDERS is DEPRECATED; set `providers_file:` in config.yaml instead (it is honored for now).\n  model m-openai-chat via openai-chat (http://127.0.0.1:1) max unbounded\nbusbar: 1 models, 0 pools"},"headers":{},"status":0}

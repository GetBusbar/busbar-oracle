{
  "_comment": [
    "Differences from the published 1.5.5 that the OWNER has accepted, each with a rationale and a kind:",
    "  improvement  additive or strictly better; a 1.5.5 client/operator keeps working unchanged",
    "A matching cell reports as ACCEPTED in its own column — never as a silent pass — so a later change",
    "to the same surface still shows. `cells` are id regexes; `classes` limits the acceptance to those",
    "divergence classes (a status or usage change on an 'improvement' entry is still red).",
    "Zero UNACCEPTED divergences is the ship gate.",
    "An entry with `transform: {candidate: [[regex, replacement], ...]}` is line-precise: the candidate's stderr/body text is rewritten before the diff, so only that token is forgiven; the cell reports ACCEPTED, never PASS.",
    "  breaking     a deliberate change named in the 1.6.0 CHANGELOG (`changelog` field required) — the ONLY kind that may accept a `status` or `effects.usage` class",
    "The differ refuses any entry that accepts `status` or `effects.usage` without kind=breaking and a `changelog` field, and any entry with neither `cells` nor `classes` (a total blanket).",
    "  EVERY entry, improvement or breaking, owes a `changelog` line that appears verbatim in CHANGELOG.md (ARCHITECTURE.md's owner rule accepts an improvement 'named in the CHANGELOG' too), or an explicit `\"changelog\": null` with a `changelog_reason`; a breaking entry may never waive. scripts/changelog-register-check.sh is the gate."
  ],
  "accepted": [
    {
      "id": "D-1 diagnostic codes",
      "kind": "improvement",
      "changelog": "Every error and warning line carries a diagnostic code.",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "rationale": "1.6.0 prefixes every [error]/[warn]/warning: stderr line with a diagnostic code (BUSBAR-NNNN:) and every boot log line with diag=BUSBAR-NNNN; the text after the code is byte-identical to 1.5.5.",
      "transform": {
        "candidate": [
          [
            "^(\\[error\\]|\\[warn\\]|warning:) BUSBAR-\\d{4}: ",
            "\\1 "
          ],
          [
            " diag=BUSBAR-\\d{4}",
            ""
          ]
        ]
      }
    },
    {
      "id": "D-2 jemalloc line is info",
      "kind": "improvement",
      "changelog": "The jemalloc background-purge line is `[info]` on macOS",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "rationale": "On macOS the jemalloc background-thread line is [info] with an explanation instead of 1.5.5's [warn]; same fallback behaviour.",
      "transform": {
        "candidate": [
          [
            "^\\[info\\] jemalloc background purge thread unavailable on this target \\((.*?)\\) — EXPECTED on macOS, which has no background-thread mallctl; busbar's idle-purge fallback keeps RSS returning to idle after a load burst$",
            "[warn] could not enable jemalloc background purge thread (\\1); enabling busbar's idle purge fallback so RSS still returns to idle after a load burst"
          ]
        ]
      }
    },
    {
      "id": "O-1 new metering series",
      "kind": "improvement",
      "changelog": "One new `/metrics` series on a 1.5.5 config:",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^ops\\.scrape\\|metrics\\||^neutrality\\|metrics-series$|^crosscut\\.traps\\|request-duration-quantiles$",
      "rationale": "One additive /metrics series on a 1.5.5 config: busbar_metering_pending_coalesced_total (write-behind overflow sentinel). No 1.5.5 series changed.",
      "transform": {
        "candidate": [
          [
            "^(# (HELP|TYPE) )?busbar_metering_pending_coalesced_total[^\\n]*\\n?",
            ""
          ]
        ]
      }
    },
    {
      "id": "F-011 additive admin views",
      "kind": "improvement",
      "changelog": "Admin views gained fields; none changed.",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^admin\\.ops\\|(GetHooksName|GetHooks)\\|ok$|^hooks\\|admin-list$|^hooks\\|register\\|plugin-alias$|^admin\\.ops\\|DeleteOverlaySection\\|not-found$|^admin\\.ops\\|(DeleteHooksName|PatchHooksNameSettings|PutHooksName|PostHooks)\\|ok$|^admin\\.ops\\|PostHooks\\|if-match-stale$",
      "expected_cells": 10,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Hook views gain fires_at/groups/phase; the overlay-section 404 lists the new sections; openapi.json adds the mcp/a2a endpoints. Every 1.5.5 field is unchanged. SPLIT 2026-09-05 (round-3 audit): effects.readback moved out of this improvement entry into F-011r. effects.readback is now a MONEY class (diff-cells.py rates it 10: it is the proof that the write the cell made actually persisted, and as what), and the readback half is not purely additive — the hook view's always-null `at` field is REMOVED in favour of fires_at, which is a declared breaking change and needs a changelog line."
    },
    {
      "id": "F-011r hook write readback: `at` gives way to `fires_at`",
      "kind": "breaking",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage; split out 2026-09-05 (round-3 audit)",
      "cells": "^admin\\.ops\\|(PostHooks|PutHooksName|PatchHooksNameSettings|DeleteHooksName)\\|ok$",
      "expected_cells": 4,
      "classes": [
        "effects.readback"
      ],
      "rationale": "The follow-up GET a hook write's cell performs reads the same hook view the GET cells read: it gains fires_at/groups/phase and LOSES the always-null `at` field. The additive half is F-011's; the removal is a declared 1.6.0 break named in the CHANGELOG's rewritten-spellings paragraph, so the readback class it moves is registered here as kind=breaking rather than folded into an improvement. Scoped to exactly the four cells that HAVE an effects.readback in the golden (admin.ops|PostHooks|ok, |PutHooksName|ok, |PatchHooksNameSettings|ok, |DeleteHooksName|ok); admin.ops|PostHooks|if-match-stale and every hooks|* cell record no readback at all. The readback's status and path are unchanged in all four — only the view's field set moves.",
      "changelog": "1.6.0 Changed: the always-null `at` field on the hook view gives way to `fires_at` (rewritten for you by --migrate-config)"
    },
    {
      "id": "F-013 validation wording",
      "kind": "improvement",
      "changelog": "Validation messages know the new keys.",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^boot\\.refusal\\|BOOT-(P20|P29|P30|P07|P09)\\|validate$",
      "expected_cells": 5,
      "classes": [
        "effects.stderr"
      ],
      "rationale": "The new token limit metrics appear in a limit's expected-one-of list; reserved-name / credential-mode sentences rephrased. Same refusal, same exit code. NARROWED 2026-09-05 (the plane-key-lift fix): the top-level plane-key row dropped — the TOP-LEVEL expected-one-of list no longer includes the plane keys, because the 1.6.0-additive keys are lifted off the document before the frozen 1.5.5-shaped parse. That cell is byte-identical to 1.5.5 now and needs no acceptance. NARROWED AGAIN 2026-09-06 (oracle-phase0, committed-golden triage) for the two cells this entry should never have carried. BOOT-020: the divergence it forgave was 'protocol list reordered', and a reorder of an operator-visible list is neither additive nor better — it is a change nobody announced. The PUBLISHED 1.5.5 binary prints `anthropic, openai, gemini, bedrock, responses, cohere`; the plugin consolidation had moved it to `anthropic, gemini, openai, …` believing that was already the shipped order. The order is RESTORED in `busbar_llm_codec::DECLS`, so BOOT-020 is byte-identical to 1.5.5 again and owes no acceptance at all. BOOT-043b: the unknown-member refusal did not merely rephrase a sentence — its FRAME LINE moved from the validate stage to the resolve stage, which an operator's log match sees; it is registered on its own terms as kind=breaking (F-013b) rather than folded into an improvement. COUNT FOLLOWS THE NARROWING: `expected_cells` is 5, one per BOOT-P id this entry still names (P20, P29, P30, P07, P09) — not the 7 it forgave before BOOT-020 and BOOT-043b were removed above. The declared width is derived from the regex it accompanies, so a later widening of one without the other is caught by the differ rather than silently forgiven."
    },
    {
      "id": "F-013b the unknown pool member is refused by the resolver, not the validator",
      "kind": "breaking",
      "by": "owner (Matthew Jackson), 2026-09-06 oracle-phase0 committed-golden triage",
      "cells": "^boot\\.refusal\\|BOOT-043b\\|validate$",
      "classes": [
        "effects.stderr"
      ],
      "rationale": "A pool that names a member nothing defines is refused by BOTH releases, with exit code 1, the same empty stdout and the same preceding BUSBAR_PROVIDERS deprecation warning — but the refusal now comes out of a DIFFERENT STAGE and says so. 1.5.5 collected it in semantic validation and framed it `[error] config validation failed:` with the sentence `pool 'oracle-unused' references unknown model 'nope'`. 1.6.0 resolves pool members against three top-level maps (`models:`, `tools:`, `agents:`) rather than one, so the check belongs to `config::resolve` — which frames its errors `[error] config errors:` — and the sentence names the config path and the three maps: `pools.oracle-unused: member `nope` is not defined in any of the top-level `models:`, `tools:`, or `agents:` maps. Define it there, or remove it from the pool.` The new sentence is strictly more useful, but the FRAME LINE is not additive: an operator or CI job that greps `config validation failed:` to catch a bad config stops matching on this class of refusal, so this is registered as a break with a migration note rather than as an improvement. SPLIT OUT of F-013 (which forgave it as a rephrasing) 2026-09-06. The `BUSBAR-3015:` prefix on the [error] line is D-1's and is already rewritten away before this entry is consulted. Scoped to the ONE recorded cell that drives an undefined pool member; a further fixture gets its own row rather than a widened regex.",
      "changelog": "1.6.0 Changed: a pool member that nothing defines is refused by the resolver, under the `config errors:` frame, naming `models:`, `tools:` and `agents:`."
    },
    {
      "id": "F-015 an inline secret is refused without echoing it",
      "kind": "improvement",
      "by": "owner (Matthew Jackson), 2026-09-06 oracle-phase0 parity triage",
      "cells": "^boot\\.refusal\\|BOOT-P32\\|validate$",
      "expected_cells": 1,
      "classes": [
        "effects.stderr"
      ],
      "rationale": "BOOT-P32 writes an INLINE LITERAL where a secret reference belongs (`providers.gemini.api_key: 7`). 1.5.5 had no scalar visitor on SecretRef, so that value fell through to serde's DEFAULT invalid-type error, which prints the value it was handed: `invalid type: integer `7`, expected a secret reference map: {…}`. The one path this type exists to keep a secret off — stderr and the boot log — is exactly where the rejected secret went, and an unquoted scalar (`api_key: 483920175534`) is the spelling nobody thinks to check. 1.6.0's SecretRef refuses every inline-scalar spelling through one message that states the RULE and never echoes the value: `a secret value must be a REFERENCE, never an inline literal (the value is not echoed): use { env: <VAR> }, { file: <path> }, or { module: <secret-module>, settings: {…} }` (crates/secret-ref/src/lib.rs, RefVisitor::inline_literal). That is the rule the config inventory already documents for every secret-bearing key, so the new sentence tells the operator what to write instead of what serde could not parse. NOT a plane-work side effect: the wording landed on 2026-08-08, long before this branch's base, and reverting it would restore a message that prints an operator's pasted API key into the log. SCOPE: the refusal itself is unchanged — exit code 1, empty stdout, the same `config.yaml: invalid YAML: providers.gemini.api_key: … at line 42 column 14` frame around the sentence, and the same preceding BUSBAR_PROVIDERS deprecation warning byte-for-byte. The `BUSBAR-3015:` prefix on the [error] line is D-1's and is already rewritten away before this entry is consulted. Narrowed to the ONE recorded cell that drives an inline-literal secret; if a fixture ever adds another, name it here explicitly rather than widening the regex to the boot.refusal family.",
      "changelog": "An inline literal where a secret reference belongs is refused without echoing it."
    },
    {
      "id": "F-013 download error wording",
      "kind": "improvement",
      "changelog": "a plugin download failure quotes the HTTP client's current error text",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^boot\\.(refusal|warning)\\|BOOT-(141|W24)\\|boot$",
      "expected_cells": 2,
      "classes": [
        "body",
        "effects.stderr"
      ],
      "rationale": "The plugin download failure quotes the HTTP client's newer error text (connect error) for the same refusal. NARROWED 2026-09-05 (the plane-key-lift fix): the two neutrality unknown-key cells dropped — NEUT-U-top's top-level list and NEUT-U-auth's auth: list are byte-identical to 1.5.5 again, because the 1.6.0-additive keys (the plane sections, and auth.policy) are lifted off the document before the frozen 1.5.5-shaped parse. Neither cell has a divergence left to waive. WIDENED 2026-09-05 (A10b): BOOT-W24's `plugins.fetch: miss on reload` warning quotes the SAME reqwest connect-error text change on the SAME download-failure path (`error sending request for url (...)` -> `client error (Connect): tcp connect error: Connection refused (os error 61)`); its `diag=BUSBAR-6001` prefix is D-1 (already covered) and reaches this class only because both fire on the same line."
    },
    {
      "id": "F-003 CLI text",
      "kind": "improvement",
      "changelog": "`--help` documents `-c`/`--config` and `--providers`; `--version` prints a second line.",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^(cli\\|(--help|-h|--version|-V)|documented\\|readme\\|env-launch)$",
      "expected_cells": 5,
      "classes": [
        "body"
      ],
      "rationale": "--help documents the new -c/--providers flags; --version prints a second build-profile line. documented|readme|env-launch (PB-71) drives the same --version path under BUSBAR_CONFIG and hits the identical second line."
    },
    {
      "id": "S-1 bedrock text blocks carry no contentBlockStart",
      "kind": "improvement",
      "changelog": "Bedrock text blocks no longer open with an empty `contentBlockStart`.",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^llm\\|bedrock\\|[a-z]+\\|request\\|ok_stream$",
      "expected_cells": 5,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: on the AWS ConverseStream wire a text block starts with its first contentBlockDelta; contentBlockStart is emitted only for tool-use blocks. 1.5.5 emitted an empty contentBlockStart for text; 1.6.0 matches AWS. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "S-2 responses stream lifecycle frames",
      "kind": "improvement",
      "changelog": "Responses API streams carry the lifecycle frames the spec declares:",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^llm\\|responses\\|[a-z]+\\|request\\|ok_stream$",
      "expected_cells": 5,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: the Responses API stream includes response.content_part.added / response.output_text.done / response.content_part.done with contiguous sequence_number; 1.5.5 omitted them. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "PR2-D1 degraded non-2xx breaker recording",
      "kind": "improvement",
      "changelog": "a non-2xx records the breaker outcome by status class, honouring the upstream `Retry-After` as the cooldown floor, and emits the upstream-failure series",
      "by": "owner (Matthew Jackson), 2026-09-04 PR-2 attempt-seam D-table decision",
      "cells": "^route\\.failover\\|fb\\|member-down$|^route\\.failover\\|fo\\|primary-429$",
      "expected_cells": 2,
      "classes": [
        "effects.metrics"
      ],
      "rationale": "A non-2xx on a fallback/least-bad/queue hop now records the breaker outcome exactly as the primary hop does (per status class, honouring the upstream Retry-After as the cooldown floor, with the upstream-failure series emitted) instead of a class-less degraded transient with no Retry-After. The client still receives the relayed upstream error; only the breaker bookkeeping and metrics changed."
    },
    {
      "id": "PR2-D2 degraded hard-down recorded",
      "kind": "breaking",
      "by": "owner (Matthew Jackson), 2026-09-04 PR-2 attempt-seam D-table decision",
      "cells": "^route\\.failover\\|fb\\|member-401$",
      "expected_cells": 1,
      "classes": [
        "status",
        "body",
        "headers",
        "effects.metrics"
      ],
      "rationale": "An auth/billing hard-down on a fallback/least-bad/queue hop now trips the lane in every breaker cell and, for busbar's own lane credential, answers with the ingress-native auth-failure envelope — the same rule the primary hop applies. Previously the degraded hop relayed the upstream 401 body verbatim and recorded nothing, leaving the dead lane Closed.",
      "changelog": "1.6.0 Improvements: a fallback hop refused upstream is answered in the ingress-native auth-failure envelope and recorded on the breaker"
    },
    {
      "id": "PR2-D3 degraded client-fault counter",
      "kind": "improvement",
      "changelog": "a client-fault 4xx bumps the lane's `client_fault` counter",
      "by": "owner (Matthew Jackson), 2026-09-04 PR-2 attempt-seam D-table decision",
      "cells": "^route\\.failover\\|fb\\|member-4[0-9][0-9]$",
      "expected_cells": 1,
      "classes": [
        "effects.metrics"
      ],
      "rationale": "A client-fault 4xx served through a fallback/least-bad hop now bumps the lane's client_fault observability counter as the primary hop does. No breaker penalty either way; the relayed response is unchanged."
    },
    {
      "id": "PR2-D4 fallback stream billed",
      "kind": "breaking",
      "by": "owner (Matthew Jackson), 2026-09-04 PR-2 attempt-seam D-table decision",
      "cells": "^route\\.failover\\|fb\\|member-down-stream-openai$",
      "expected_cells": 1,
      "classes": [
        "body",
        "effects.egress",
        "effects.metrics",
        "effects.usage"
      ],
      "rationale": "OWNER DECISION: bill them. A streaming request served through a fallback/least-bad/queue hop to an OpenAI Chat Completions lane now has stream_options.include_usage injected upstream exactly as a primary hop does, so the trailing usage chunk arrives and the key is billed its real tokens (previously zero). The client-facing stream is unchanged for a client that did not opt in (the injected chunk is stripped at the framing seam); an opted-in client now receives its usage chunk on the fallback hop too. CHANGELOG 1.6.0: 'Fallback-pool and least-bad streams to OpenAI Chat Completions lanes are now metered (previously billed zero tokens).' The egress record shows the injected stream_options.include_usage on the fallback hop.",
      "changelog": "1.6.0 Improvements: fallback-lane streams are now billed"
    },
    {
      "id": "PR2-D5 degraded attempt-timeout member override",
      "kind": "improvement",
      "changelog": "a pool member's `attempt_timeout_ms` override is honoured",
      "by": "owner (Matthew Jackson), 2026-09-04 PR-2 attempt-seam D-table decision",
      "cells": "^route\\.failover\\|fb\\|member-slow$",
      "expected_cells": 0,
      "classes": [
        "effects.metrics"
      ],
      "rationale": "The pool-member attempt_timeout_ms override is now honoured on fallback/least-bad hops (the degraded path used only the lane-level value). The cap can only fire earlier where an operator set a tighter member override."
    },
    {
      "id": "PR2-D9 degraded transport-error telemetry",
      "kind": "improvement",
      "changelog": "a transport error emits the same upstream-failure and failover series",
      "by": "owner (Matthew Jackson), 2026-09-04 PR-2 attempt-seam D-table decision",
      "cells": "^route\\.failover\\|fb\\|member-down$|^route\\.failover\\|fb\\|all-down$|^route\\.failover\\|lb\\|member-down$",
      "expected_cells": 3,
      "classes": [
        "effects.metrics"
      ],
      "rationale": "A transport error (connect refused / reset / timeout) on a fallback/least-bad/queue hop now emits the same upstream-failure and failover series the primary hop emits. Breaker recording and the client response are unchanged."
    },
    {
      "id": "S-3 bedrock metrics member",
      "kind": "improvement",
      "changelog": "A Bedrock `Converse` response always carries `metrics`.",
      "by": "owner (Matthew Jackson), 2026-09-04 maximum spec compliance",
      "cells": "^llm\\|bedrock\\|bedrock\\|request\\|ok$",
      "expected_cells": 1,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "The Converse output shape requires metrics; 1.5.5's same-dialect passthrough dropped it. 1.6.0 always carries it (latencyMs is timing and normalized)."
    },
    {
      "id": "S-4 anthropic required members",
      "kind": "improvement",
      "changelog": "An Anthropic response carries the members its published schema requires.",
      "by": "owner (Matthew Jackson), 2026-09-04 maximum spec compliance",
      "cells": "^llm\\|anthropic\\|(bedrock|cohere|gemini|openai|responses)\\|request\\|ok(_stream)?$",
      "expected_cells": 9,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Anthropic's published Message / stream-event schemas mark stop_details, container, citations, the cache and service_tier usage members, output_tokens_details and server_tool_use required; 1.5.5 omitted them. 1.6.0 emits the spec's null/zero defaults or the carried value."
    },
    {
      "id": "S-5 openai chat required members",
      "kind": "improvement",
      "changelog": "A chat completion's `logprobs` object carries `refusal`.",
      "by": "owner (Matthew Jackson), 2026-09-04 maximum spec compliance",
      "cells": "^llm\\|openai\\|(anthropic|bedrock|cohere|gemini|responses)\\|request\\|ok$|^route\\.failover\\|(fo\\|primary-(429|5xx|down)|fo\\|second-request-after-trip|lb\\|up)$",
      "expected_cells": 10,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "choices[].logprobs and message.refusal are required by the published chat schema; null when the backend carried none."
    },
    {
      "id": "S-6 responses required members",
      "kind": "improvement",
      "changelog": "A Responses object carries the members its published schema requires.",
      "by": "owner (Matthew Jackson), 2026-09-04 maximum spec compliance",
      "cells": "^llm\\|responses\\|(anthropic|bedrock|cohere|gemini|openai)\\|request\\|ok$",
      "expected_cells": 5,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "The Response object's required members (created_at, error, incomplete_details, instructions, tools, tool_choice, parallel_tool_calls, metadata, temperature, top_p, part logprobs, usage details) are emitted with spec defaults or carried values."
    },
    {
      "id": "F-011b openapi.json additive endpoints",
      "kind": "improvement",
      "changelog": "`openapi.json` describes the MCP and A2A endpoints",
      "by": "owner (Matthew Jackson), 2026-09-04 parity triage",
      "cells": "^admin\\.ops\\|GetOpenapiJson\\|ok$|^crosscut\\.traps\\|openapi-info-version$|^neutrality\\|routes\\|admin-openapi-paths$",
      "expected_cells": 3,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "openapi.json adds the mcp/a2a endpoints; every 1.5.5 path and schema is unchanged (a dedicated cell pins info.version). The document also gains a Vary header and reports the binary's true version in info.version (1.5.5 shipped a stale 1.5.4)."
    },
    {
      "id": "S-7 responses-door streams stream",
      "kind": "improvement",
      "changelog": "A `stream: true` request on the Responses door is answered as a stream.",
      "by": "owner (Matthew Jackson), 2026-09-04 maximum spec compliance",
      "cells": "^llm\\|responses\\|responses\\|request\\|ok_stream$",
      "expected_cells": 1,
      "classes": [
        "body",
        "headers",
        "norm.rules"
      ],
      "rationale": "A stream: true request on the Responses door is served as text/event-stream with the ResponseStreamEvent sequence; 1.5.5 answered the same-dialect case with a buffered JSON body. Metered identically (250 cents / 18 tokens). The buffered 1.5.5 body carried a synthesized content-length with no transfer-encoding; the SSE body is chunked, so the normalizer's header-shape rule fires where it did not before (norm.rules) -- a byproduct of the same content-type change, not a separate behavior."
    },
    {
      "id": "E7-1 non-Responses doors stream when the backend is Responses-shaped",
      "kind": "improvement",
      "changelog": "Every door streams when the routed lane is Responses-shaped.",
      "by": "owner (Matthew Jackson), 2026-09-04 E7 conformance-rig triage",
      "cells": "^llm\\|(cohere|gemini|openai)\\|responses\\|request\\|ok_stream$",
      "expected_cells": 3,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "A stream: true request whose routed lane is Responses-shaped is now served as text/event-stream carrying the ingress door's own translated stream frames (Cohere v2 SSE, Gemini's SSE framing, OpenAI chat.completion.chunk SSE); 1.5.5 answered it as a buffered application/json body regardless of the door that received it. The Anthropic- and Bedrock-door cells for the same egress already carry this fix under S-4's and S-1's broader per-door wildcards; this entry covers the three doors (Cohere, Gemini, OpenAI chat) whose own required-member registrations (S-5; none was needed for Cohere/Gemini, which had zero required-member violations) did not include the ok_stream outcome. Metered identically (250 cents / 18 tokens)."
    },
    {
      "id": "S-8 streamed usage carries the same attribution buckets as buffered",
      "kind": "improvement",
      "changelog": "A streamed response's `usage` carries the same attribution buckets as a buffered one.",
      "by": "owner (Matthew Jackson), 2026-09-05 llm-codec audit",
      "cells": "^llm\\|[a-z_]+\\|responses\\|request\\|ok_stream$",
      "expected_cells": 6,
      "classes": [
        "body"
      ],
      "rationale": "NARROWED 2026-09-05 (round-3 audit): was `^llm\\|[a-z_]+\\|[a-z_]+\\|request\\|ok_stream$`, which forgave a body change on EVERY streamed llm cell (32 of them) when the fix can only move the cells whose upstream actually reports an attribution sub-bucket on the streamed path. In this harness that is exactly the six `<door>|responses` cells: mock-upstream.py's /v1/responses is the only dialect whose usage object carries populated sub-buckets (input_tokens_details.cached_tokens / cache_write_tokens, output_tokens_details.reasoning_tokens); anthropic_stream, openai_chat_stream, gemini_stream, cohere_stream and bedrock_stream all send the totals with every sub-bucket null or absent, so no other ok_stream cell has a bucket to gain. `llm|responses|responses|request|ok_stream` is the same-protocol A-tap half of the fix; the other five are the shared trailing-usage fold half. If a fixture ever starts sending sub-buckets on another dialect's stream, add that cell here explicitly rather than restoring the wildcard. ORIGINAL RATIONALE: A stream's usage object now reports every per-dialect attribution sub-bucket the provider sent (reasoning tokens, the two Anthropic cache-creation TTL tiers and its web-search/service-tier pair, the four OpenAI audio and predicted-outputs slices, Gemini's tool-use prompt slice, Cohere's separately-metered billed_units trio), instead of reporting them only when the same request was made with stream:false. 1.5.5 carried NO sub-buckets on either path, so nothing a 1.5.5 client read has changed value or disappeared — the buckets are additive members that were absent before; 1.6.0 recovered them on the buffered path and this closes the streamed half of the same gap, so the two answers for one request finally agree. The totals are untouched: billable_tokens ignores the whole attribution struct. CAVEAT for a future re-record: Cohere's billed_units trio is the one bucket that is NOT a slice of the totals — to_token_usage lets billed_input/billed_output win over the raw counts — so a Cohere backend that reports billed_units on a STREAM will move that stream's ledgered figures to the same numbers its buffered twin already ledgers. No recorded cell exercises it (the oracle mock sends no billed_units on a stream), so this entry accepts `body` only; should a fixture ever start sending it, the effects.usage delta must be re-registered as kind=breaking with a CHANGELOG line rather than folded in here."
    },
    {
      "id": "M-1 refund reaches the cell the charge reached",
      "kind": "improvement",
      "changelog": "A failed request's flat fee is refunded even when its window rolled under it.",
      "by": "owner (Matthew Jackson), 2026-09-05 maximum spec compliance",
      "cells": "^billing\\|key-usage\\|refund-across-window$",
      "expected_cells": 1,
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "NARROWED 2026-09-05 (round-3 audit): was `^(billing|limits)\\|`, which forgave a body or header change on EVERY billing and limits cell — i.e. it would have waived a WRONG BILLING FIGURE in any of those response bodies, the exact class of bug this register exists to surface. The entry's own rationale already states the served change is a no-op on every cell the oracle records (no scripted cell drives a window straddle), so the correct scope is the ONE future cell that will drive it: billing|key-usage|refund-across-window (defined SKIP-able in cells.json; the recorder owes it once the mock can drive a concurrent window roll). Until that cell is recorded this entry matches nothing, which is the point. ORIGINAL RATIONALE: A request pinned to window M whose charge landed in place on a cell a concurrent admission had already rolled to M+1 now has its flat per-request fee refunded from that same cell. 1.5.5 resolved a charge on `window > cell.window_start` (charge in place on a rolled cell) but a refund on `cell.window_start == window`, so the two halves of one request could land on different cells and a failed straddling request kept its fee for the life of that window -- leaving the derived spend a budget cap reads one fee too high. refund_bucket now mirrors resolve_for_charge exactly (`cell.window_start >= window`): the refund reaches the cell the charge reached. A cell genuinely OLDER than the request's window is still a no-op under both rules. Strictly in the client's favour and only ever reachable on a boundary straddle, which no scripted oracle cell drives -- the recorded billing and limits cells are unchanged. The divergence class it WOULD produce is effects.usage, and this register's own rule reserves that class for a kind=breaking entry naming a CHANGELOG line, so if a cell ever does show it the entry must be promoted by the owner rather than widened here."
    },
    {
      "id": "M-2 a mid-stream upstream failure is not billed as a completion",
      "kind": "improvement",
      "changelog": "A stream that dies mid-flight is no longer served, and billed, as a completed one.",
      "by": "owner (Matthew Jackson), 2026-09-05 llm-codec audit",
      "cells": "^llm\\|[a-z_]+\\|(openai|cohere)\\|request\\|mid-stream-error(_stream)?$",
      "expected_cells": 0,
      "classes": [
        "body"
      ],
      "rationale": "NARROWED 2026-09-05 (round-4 audit): was `^llm\\|`, which forgave a body change on every one of the 127 recorded LLM cells -- any writer regression whose only symptom is a wrong response body would have reported PASS/ACCEPTED under this entry. The entry's own rationale states no recorded cell exercises either shape, so the honest scope is the two future cells that will (an inline `data: {\"error\":...}` chunk on an OpenAI-compatible stream, a terminal `finish_reason: \"ERROR\"` on a Cohere stream); until they are recorded this entry matches nothing. ORIGINAL RATIONALE: Two upstream mid-stream failure shapes were served, and billed, as clean successes. (1) An OpenAI-compatible backend (OpenAI, Azure, vLLM, OpenRouter) that fails AFTER its 200 headers are on the wire delivers the failure inline as a `data: {\"error\":{...}}` SSE chunk carrying no `choices`; the openai_chat stream reader guarded only on the [DONE] sentinel and `choices`, so the chunk decoded to nothing at all -- no IrStreamEvent::Error, so terminal_error() stayed None, the breaker recorded no fault, the partial completion was billed in full, and a cross-protocol client got balanced block-stops plus an ordinary terminator with no hint the answer was truncated. The gemini, bedrock, anthropic and openai_responses readers each already pushed an Error for their equivalent frame. (2) A Cohere stream ending on the generic infra `finish_reason: \"ERROR\"` decoded to IrStopReason::Error but pushed no Error event, and no other dialect's writer has a native token for that reason, so every cross-protocol writer rendered it as a success terminator (stop/end_turn) -- the same outcome by a different route. 1.5.5 had the identical gap (v1.5.5:crates/busbar/src/proto/openai_chat/reader.rs read_response_events carries the same two guards and no error arm), so this is a deliberate divergence from the pin, named in the 1.6.0 CHANGELOG under Improvements. Strictly in the operator's and the client's favour: a failed request stops being charged for, and a failing lane finally trips. ERROR_TOXIC is deliberately untouched -- it maps to IrStopReason::Safety, a content-moderation refusal is a correctly-served response, and faulting a lane for one would trip the breaker on ordinary safety behaviour. No successful stream changes on any dialect. NO RECORDED CELL EXERCISES EITHER SHAPE: the oracle mock has no mock_control verb that injects an inline error chunk or a terminal ERROR finish_reason (the closest, `cut`, severs the body rather than sending an error frame), so the recorded llm family is unchanged. The divergence classes this WOULD produce on such a cell are `status` and `effects.usage`, and this register's own rule reserves both for a kind=breaking entry naming a CHANGELOG line; this entry therefore accepts `body` only, and if a fixture is ever added that drives one of these shapes the entry must be promoted by the owner rather than widened here."
    },
    {
      "id": "M-3 Cohere billed_units bill the same buffered and streamed",
      "kind": "breaking",
      "by": "owner (Matthew Jackson), 2026-09-05 round-3 audit",
      "cells": "^llm\\|[a-z_]+\\|cohere\\|request\\|ok(_stream)?$",
      "expected_cells": 12,
      "classes": [
        "effects.usage",
        "body"
      ],
      "rationale": "NARROWED 2026-09-06 (shadow-oracle round-5): the `cells` regex was `^llm\\|[a-z_]+\\|cohere\\|request\\|ok(_stream)?$|^billing\\|`. That trailing `|^billing\\|` alternation held this entry's `effects.usage` -- a MONEY class -- over all twelve `billing|*` cells (eleven of them owed), a family this entry's id, rationale and changelog line never mention. Those cells are the ledger's own views (key/group/admin usage, the refund window); a change to what busbar CHARGES, or to what it reports having charged, is precisely the divergence the oracle exists to catch, and it would have reported ACCEPTED under a Cohere waiver. Cohere reaches the billing views only through the same `llm|*|cohere|*` cells already named on the left of the alternation, so the honest scope is exactly those twelve and nothing else; `billing|key-usage|refund-across-window` has its own entry (M-1). The register now requires every entry to declare `expected_cells`, so an alternation bolted onto a live scope stops the run. ORIGINAL RATIONALE: Cohere reports usage TWICE -- a raw `usage.tokens` bucket and a separately-metered `usage.billed_units` bucket -- and billed_units is the count Cohere itself invoices the operator on. 1.5.5 read only `tokens` (its cohere reader parsed usage.tokens on both the stream and the non-stream path; billed_units was never read) and billed those. 1.6.0 lets billed_input_tokens/billed_output_tokens win over the raw totals inside IrUsage::to_token_usage, the one projection the ledger reads. This entry covers the BUFFERED arm finally reaching that same projection: chat_usage (the projection behind ChatRespHandle::billing()) had been rebuilding TokenUsage from the RAW IrUsage totals, so a single completion was billed the billed counts when streamed and the raw counts when buffered -- two different invoices for one completion, decided only by the caller's stream flag. Both arms now answer identically. No other dialect populates the billed fields, so every other backend's ledgered counts stay byte-identical to 1.5.5. The oracle's own mock sends billed_units EQUAL to tokens, so no recorded cell moves today; the entry is registered ahead of any fixture that makes them differ, which is exactly what S-8's caveat requires.",
      "changelog": "1.6.0 Improvements: a Cohere backend's `usage.billed_units` is what the key is billed, on both the buffered and the streamed path"
    },
    {
      "id": "F-014 Bedrock totalTokens counts the cache tokens AWS counts",
      "kind": "improvement",
      "by": "owner (Matthew Jackson), 2026-09-05 round-3 audit",
      "cells": "^llm\\|bedrock\\|[a-z_]+\\|request\\|ok(_stream)?$",
      "expected_cells": 11,
      "classes": [
        "body"
      ],
      "rationale": "AWS Converse reports inputTokens as the NON-cached input only and documents the full input as inputTokens + cacheReadInputTokens + cacheWriteInputTokens (its worked caching example totals 16 input + 4 output + 695 cache-write as totalTokens 715). 1.5.5's Bedrock writer derived totalTokens = inputTokens + outputTokens while emitting the two cache counts beside it as ADDITIVE siblings, so on a cache-bearing response the published parts summed PAST the published total and a consumer reading totalTokens -- the field AWS points accounting consumers at -- lost every cache token. Both write sites (the buffered Converse body and the streamed metadata frame) now publish the AWS sum. The component fields are untouched, a response with no cache activity is byte-identical to 1.5.5 (the omitted cache buckets add zero), and this is a Bedrock-INGRESS body shape only: nothing Busbar ledgers reads totalTokens, since the Bedrock reader never parses it. The oracle's mock reports no cache tokens, so no recorded cell moves today.",
      "changelog": "Bedrock `usage.totalTokens` counts the cache tokens AWS counts."
    },
    {
      "id": "A17 Anthropic billing_error names its own error type",
      "kind": "improvement",
      "by": "owner (Matthew Jackson), 2026-09-05 oracle Phase 0 group-budget-total cell",
      "cells": "^llm\\|anthropic\\|anthropic\\|request\\|over_budget_total$",
      "expected_cells": 1,
      "classes": [
        "body"
      ],
      "rationale": "1.5.5 answers the group's own BUDGET-total exhaustion (metric: budget, not the requests-cap `over_budget` arm) with error.type \"insufficient_quota\" — OpenAI's vocabulary, outside the nine types the published Anthropic error envelope declares. 1.6.0 answers the identical 429 with error.type \"billing_error\", the Anthropic-native member for a billing/quota refusal. Status (429), message text, and every other field are byte-identical on both sides; only /error/type differs: \"insufficient_quota\" (1.5.5) -> \"billing_error\" (1.6.0).",
      "changelog": "An Anthropic-dialect error names an Anthropic error type. Busbar's quota and context-overflow refusals reached an Anthropic client as `insufficient_quota` and `context_length_exceeded` — OpenAI's vocabulary, and outside the nine types the published error envelope declares, so the official SDK raised a generic `APIError`. They are now `billing_error` and `invalid_request_error`. The status, message and every other error type are unchanged."
    },
    {
      "id": "S-9 responses url citations survive the hop",
      "kind": "improvement",
      "changelog": "**A Responses URL citation survives the hop.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|(anthropic|bedrock|cohere|gemini|openai|responses)\\|responses\\|request\\|ok(_stream)?$",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: the Responses API's published UrlCitationBody FLATTENS url/title/start_index/end_index onto the annotation, while Chat nests them under a url_citation object. 1.5.5 wrote the flat shape but read only the nested one, so every URL citation on a Responses-shaped upstream response was dropped -- including Responses->Responses, where the reader consumed bytes the writer had just produced. 1.6.0 reads both published shapes, so a citation the upstream sent reaches the client. Additive: a response that carried no citation is byte-identical. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "S-10 a cachePoint before a bedrock document does not double the document",
      "kind": "improvement",
      "changelog": "**A Bedrock `cachePoint` before a document no longer sends the document twice.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|[a-z]+\\|bedrock\\|request\\|ok(_stream)?$",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: the Converse reader parks a native document/video block at its WIRE index and also pushes a modelled block into the IR; the writer suppresses the modelled copy when it recognizes the parked one. A cachePoint or guardContent occupies a wire slot but yields no IR block, so past it the wire and IR indices disagree and the suppression missed -- the document was emitted twice, once modelled and once spliced. AWS reads the attachment twice and the caller pays its input tokens twice. 1.6.0 records the two index spaces separately so the block is emitted exactly once. Request-body only; no usage or billing class is waived here -- the double charge is the defect being removed, not an accepted difference."
    },
    {
      "id": "S-11 a refused responses function-call open emits no output_item.added",
      "kind": "improvement",
      "changelog": "**A Responses function-call item the writer refused to open no longer announces itself.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|(anthropic|bedrock|cohere|gemini|openai|responses)\\|responses\\|request\\|ok_stream$",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: the Responses stream writer bounds the number of concurrently open function-call items (MAX_OPEN_TOOLS) so a pathological backend cannot grow per-stream state without limit. The cap refused the open but the frame went out anyway: the sibling text and reasoning arms ask their open helper for a verdict and emit nothing when it refuses, while the function-call arm called the helper for its side effect and discarded the answer. Past the cap the wire therefore carried a response.output_item.added for an item that was never tracked -- so its BlockStop found nothing to close, no response.output_item.done ever followed, and the item was absent from the terminal output[] too: a lifecycle open, on the published item union, for an item that never opened and never ends. 1.6.0 decides the frame and the open together. Additive below the cap: a stream with at most MAX_OPEN_TOOLS function-call items is byte-identical. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "S-12 a deferred anthropic redacted_thinking start no longer orphans its stop",
      "kind": "improvement",
      "changelog": "**An Anthropic `redacted_thinking` block closes only if it opened.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|anthropic\\|(anthropic|bedrock|cohere|gemini|openai|responses)\\|request\\|ok_stream$",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: the published ContentBlockStartEvent carries a redacted_thinking block's opaque `data` INLINE on the start and declares no delta member for it, so busbar defers that start to the RedactedReasoningDelta that carries the bytes. 1.5.5 marked the index OPEN at the BlockStart -- before the frame existed -- which is the one thing the writer's own orphan-stop guard must not do: the guard's whole job is to suppress a content_block_stop for a block the client never saw opened, and the deferral made it answer for a frame that had not been written. A stream ending between the BlockStart and its delta (a truncation, an upstream error, or the translator draining its own open set) therefore sent a bare content_block_stop -- exactly the orphan close the guard exists to prevent, and outside the spec's 1:1 start/stop pairing. 1.6.0 marks the index open in the code path that WRITES the start, so start and stop are decided by the same fact; the same boolean stops a second redacted delta from writing a duplicate, unpaired start. Byte-identical whenever the delta arrives, which is every non-truncated redacted stream. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "S-13 folded chat usage un-folds without a finish_reason beside it",
      "kind": "improvement",
      "changelog": "**A streamed chat `usage` trailer arrives even when the stream ended without a stop reason.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|openai\\|(anthropic|bedrock|cohere|gemini|openai|responses)\\|request\\|ok_stream$",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: published include_usage says the token counts ride an ADDITIONAL final chunk whose choices array is always EMPTY. The Chat writer cannot emit two frames for one IR MessageDelta, so it folds the counts onto the chunk it does write and a downstream seam un-folds them into that trailing chunk. The writer folds whenever the terminal counts are nonzero -- NOT only when the delta carries a stop reason -- but both un-fold seams additionally required a non-null finish_reason beside the folded object. A backend that ends a stream with no stop reason (an incomplete Responses stream whose incomplete_details.reason the published spec does not name) produces exactly the chunk neither seam looked at: a client that asked for usage never received it, and a client that opted OUT was sent usage it had not asked for. Both seams now key on the folded object itself, which is only ever present because the writer put it there. Cross-dialect stream bodies only; the same-dialect byte-exact passthrough never reaches these seams, and no usage or billing class is waived -- busbar's own metering reads the IR-side tap upstream of this seam and never moved. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "S-14 a gemini speech response with no audio part is refused, not served as an mp3",
      "kind": "improvement",
      "changelog": "**A Gemini speech response that carries no audio is no longer served as an MP3.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|[a-z]+\\|gemini\\|speech\\|",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: Gemini returns synthesis as JSON with the audio at candidates[0].content.parts[0].inlineData; a mock or a direct passthrough returns the raw audio container instead, so the reader tries JSON and falls back to the bytes. 1.5.5's fallback caught the wrong bodies too: ANY body that parsed as JSON but carried no inlineData -- an error envelope, a SAFETY-blocked candidate, a text-only answer -- was handed back as a 200 whose Content-Type said audio/mpeg and whose bytes were a JSON object. A player could not open it and the upstream's own explanation was unreachable behind an audio label. 1.6.0 refuses a JSON body with no audio part (CodecError::Malformed, which the buffered seam turns into the upstream-shaped error) and leaves the not-JSON-at-all arm exactly as it was, so every real audio container reads byte-identically. This entry matches no cell in today's enumeration -- the llm cells all carry op=chat and no fixture drives the TTS leaf -- and is registered ahead of any speech fixture that would make it visible, the same posture S-8's caveat requires. No status or usage class is waived: the refusal REPLACES a 200 that was already the wrong answer. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    },
    {
      "id": "S-15 a duplicate message start keeps the stream id it opened with",
      "kind": "improvement",
      "changelog": "**One stream states one id.**",
      "by": "owner (Matthew Jackson), 2026-09-06 round-5 codec audit",
      "cells": "^llm\\|(anthropic|cohere|gemini|openai|responses)\\|(anthropic|bedrock|cohere|gemini|openai|responses)\\|request\\|(ok_stream|ok_stream_array|stream_upstream_error)$",
      "classes": [
        "body",
        "headers"
      ],
      "rationale": "Spec fidelity: a stream is not guaranteed to carry exactly one IR MessageStart. Five of the six readers gate it on their own started flag, but the Anthropic reader emits it 1:1 with the upstream message_start frame, so ANY egress fed from an Anthropic ingress can see two. 1.5.5's Anthropic, OpenAI-Chat, Gemini and Cohere writers each answered the second one by synthesizing a FRESH id, and the Responses writer latched only its sequence_number reset, not response.id -- so one message announced itself twice under two different identities. Every published streaming contract here fixes the id for the life of the message (a native OpenAI stream repeats ONE id on every chunk; a native Gemini stream's responseId is constant; the Responses SDK reads event.response.id on the terminal event to finalize), and the official SDKs latch the id from the FIRST frame that supplies it -- so the client was left correlating against an id nothing else in the stream ever mentions, and the Responses duplicate additionally disagreed with its own response.completed. 1.6.0 gives each of the five writers a per-stream id cell captured on the opening event and replayed thereafter, carried across a mid-stream Protocol::clone for the same reason the sibling open-block state already is. Byte-identical on every stream with a single opening event, which is every stream from the five gating readers. Stream bodies only; no status or usage class is waived. Owner rule: maximum spec compliance wins over 1.5.5 bytes."
    }
  ]
}
